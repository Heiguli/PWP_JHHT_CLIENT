Install requirements by: pip install -r requirements.txt

Then run in cmd or powershell or whatever console you are using: streamlit run app.py

You can run this locally in your own computer and access to Beatify API client!!
